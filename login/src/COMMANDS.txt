═══════════════════════════════════════════════════════
  DeTECHtive - Terminal Commands
═══════════════════════════════════════════════════════

📋 COPY & PASTE THESE COMMANDS:

──────────────────────────────────────────────────────
1. FIRST TIME SETUP
──────────────────────────────────────────────────────

npm install

(Wait for installation to complete - takes 2-5 minutes)


──────────────────────────────────────────────────────
2. START DEVELOPMENT SERVER
──────────────────────────────────────────────────────

npm run dev

(Then open: http://localhost:5173)


──────────────────────────────────────────────────────
3. OPTIONAL: BUILD FOR PRODUCTION
──────────────────────────────────────────────────────

npm run build

(Creates optimized build in /dist folder)


──────────────────────────────────────────────────────
4. OPTIONAL: PREVIEW PRODUCTION BUILD
──────────────────────────────────────────────────────

npm run preview


──────────────────────────────────────────────────────
5. TROUBLESHOOTING COMMANDS
──────────────────────────────────────────────────────

# If port 5173 is busy, use different port:
npm run dev -- --port 3000

# Clean install (if errors occur):
rm -rf node_modules package-lock.json
npm install

# Windows clean install:
rmdir /s /q node_modules
del package-lock.json
npm install


──────────────────────────────────────────────────────
6. BACKEND CONNECTION (OPTIONAL - PYTHON FLASK)
──────────────────────────────────────────────────────

# Install Python dependencies:
pip install flask flask-cors

# Run backend (in separate terminal):
python backend.py


──────────────────────────────────────────────────────
7. BACKEND CONNECTION (OPTIONAL - PYTHON FASTAPI)
──────────────────────────────────────────────────────

# Install Python dependencies:
pip install fastapi uvicorn python-multipart

# Run backend (in separate terminal):
uvicorn main:app --reload --port 5000


═══════════════════════════════════════════════════════
  THAT'S IT! 
  
  Full documentation: see START_HERE.md
═══════════════════════════════════════════════════════
